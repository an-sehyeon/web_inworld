<!doctype html>
<html>
<head>
    <title>Global Function</title>
</head>
<body>
</body>
<script>
    // 1. eval
    // 사용자에게 수식을 입력받아 수행하는 계산기 만들기
    var PPP = "장난하지마";
    var exp = prompt("난 최상의 계산기(Son)입니다. 수식을 넣어보십시오.");
    document.write("<h3>귀하가 입력한 수식은 " + exp + "</h3>");
    document.write("<h3>수많은 연산 끝에 결과가 나왔습니다.</h3>");
    document.write("<h1>결과는 바로 " + eval(exp) + "</h1>");

    // 2. parseInt
    var result1 = parseInt("1236");
    var result2 = parseInt("345.3");  // 345? 오류? => 345
                                      // 소수입력은 parseFloat
    var result3 = parseInt("Dave the Dive");  // 오류? 용서? => 용서(NaN)

    document.write("<h1>parseInt 결과 : " + result1 + "</h1>");
    document.write("<h1>parseInt 결과 : " + result2 + "</h1>");
    document.write("<h1>parseInt 결과 : " + result3 + "</h1>");

    // 3. isNaN
    if(isNaN(result3)){
        document.write("<h2>엄마 쟤 숫자 아닌거 넣었어요</h2>");
    }

    // 4. isFinite
    if(!isFinite(result3)){  // ! => not
        document.write("<h2>숫자 아니네</h2>");
    }

    // 5. 번외편
    if(1 == "1"){
        document.write("숫자1과 문자'1'은 같습니다.");
    }

    document.write("<br>");

    if(!(1 === "1"))
        document.write("숫자1과 문자'1'은 완벽하게 똑같진 않습니다.");
</script>
</html>